# DroolsRuleEngineExamples


 **Drools is a Business Logic integration Platform (BLiP). Its standard way to handle the business logic
 **Note: Have to add in META-INF folder: kmodule.xml
 Format:
 
 rule  <rule_name>
     <attribute> <value>
  when
        <conditions>
  then
        <actions>
  end
 
